#include <stdio.h>

// Общая таблица для любого типа данных
// ----------------------------------------
// 1. У каждого типа есть нижняя граница.
// 2. У каждого типа есть верхняя граница.
// 3. У каждого типа есть модификатор (алиас / alias).
// 	1. Signed - со знаком (знак минус -, можем использовать отрицательные числа).
// 	2. Unsigned - без знака (только положительные числа, включая ноль).

void main (void)
{
	return;
}

